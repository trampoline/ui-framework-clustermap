{
  "author": "craig mcmillan <craig@trampolinesystems.com>",
  "name": "clustermap2-ui",
  "description": "brunch + a bunch of stuff",
  "version": "0.0.3",
  "homepage": "https://github.com/trampoline/clustermap2-ui",
  "repository": {
    "type": "git",
    "url": "https://github.com/trampoline/clustermap2-ui"
  },
  "engines": {
    "node": "~0.12"
  },
  "scripts": {
    "start": "node node_modules/.bin/brunch watch --server",
    "test": "node node_modules/.bin/testacular start test/testacular.conf.js"
  },
  "dependencies": {
    "brunch": ">= 1.8 < 1.9",

    "javascript-brunch": ">= 1.7 < 1.8",
    "coffee-script-brunch": ">= 1.8 < 1.9",

    "css-brunch": ">= 1.7 < 1.8",
    "less-brunch": ">= 1.8 < 1.9",

    "auto-reload-brunch": ">= 1.7 < 1.8",

    "uglify-js-brunch": ">= 1.7 < 1.8",
    "clean-css-brunch": ">= 1.7 < 1.8",

    "jade-brunch": ">= 1.8 < 1.9",
    "static-jade-brunch": "git+ssh://git@github.com:mccraigmccraig/static-jade-brunch.git",


    "bower": ">= 1.4 < 1.5",

    "geojson-utils": ">= 1.1 < 2.0"
  }
}
